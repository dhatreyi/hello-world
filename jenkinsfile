pipeline{
    agent any
      stages{
        stage("SCM Checkout"){
           steps{
              echo "git checkout"
           }
        }
        stage("Code Build"){
           steps{
             sh "mvn clean package"
           }
        }
      }
}
